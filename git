# Work Locally
git init [dir name] - Create repository

git add <file/dir name> - Stage file or directory for commit
        -A - Stage all files in repo for commit

git commit - Commit staged files
        -m "Message" - add committ message
        --amend - update previous commit

git checkout - pull file from repo

# Work Remotely

git clone <repo URL> - Clone repository form URL

git remote add origin https://github.com/colpanick/<repo>.git - Add remote repository 

git push -u origin master
